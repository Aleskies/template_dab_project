Carpeta `data/` con subcarpetas:

- raw/: datos originales
- processed/: datos limpios y listos para entrenamiento
- external/: datos externos (diccionarios, enriquecimientos)

No subas datos sensibles al repositorio; usa `.gitignore` para excluir `data/raw/` si contiene datos privados.
